version: "3.9"

services:

  fake-news-app:

    build: .

    container_name: fake-news-detection

    restart: always

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    environment:
      - FLASK_ENV=production
